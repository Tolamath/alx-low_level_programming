#include "main.h"
/**
 * add - adds two integers and return the result
 * @a: is one of the values passed in the function add
 * @b: is one fo the values passed in the function add
 *
 * Return: 0 Always (success)
 */
int add(int a, int b)
{
	int sum;

	sum = (a + b);

	return (sum);
}
